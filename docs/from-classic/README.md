# Research carried over from ElliotWood/Forever master

These notes were written while ElliotWood/Forever ran Forever on the wowsims/classic codebase (its master
branch). The findings are about the game (beta client values, talent numbers, where data comes from) and
hold here; the file paths they cite (`sim/<class>/*.go`, `ui/core/talents/trees`) are master's, not this
branch's.

- `DATA_SOURCES.md` - where item, spell and talent data comes from and how server-side mechanics get settled
- `talent-check-2026-09-17.md`, `talent-values-2026-09-17.md` - talent trees and values checked against the beta
- `beta-pass/<class>.md` - per-class beta pass: what was verified, what changed, what is still open
